.row-fluid
	.hero-unit
		%h1 Welcome to Source.
		%p We're a community dedicated to learning, sharing, and self-discovery.
		%p
			=link_to 'Learn more', '#', :class => 'btn-primary btn-large offset2'
.row-fluid
	- @posts.each do |f|
		.span4.postbox
			.bump
				%h2.divide= f.title
				%p= f.body
				%p.offset4
					=link_to 'More...', f, :class => "btn"


- if user_signed_in?
	= link_to 'New Post', new_post_path