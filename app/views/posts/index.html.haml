%h1 src
%ul#blog-list
  - @posts.each do |post|
    %li
      %h2.post-title= link_to post.title, post
      .post-body
        = post.body
%br/
- if user_signed_in?
  = link_to 'New Post', new_post_path